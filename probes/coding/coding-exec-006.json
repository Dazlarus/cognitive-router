{
  "id": "coding-exec-006",
  "generation": "gen-2026-09-08b",
  "subIntent": "quick",
  "prompt": "Write a Python function min_path_sum(grid: list[list[int]]) -> int that finds the minimum sum of a path from the top-left to bottom-right of a grid, moving only right or down. Each cell contains a non-negative integer cost.",
  "language": "python",
  "entry": "min_path_sum",
  "harness": {
    "setup": "from solution import min_path_sum",
    "cases": [
      { "call": "min_path_sum([[1,3,1],[1,5,1],[4,2,1]])", "expect": "7" },
      { "call": "min_path_sum([[1,2,3],[4,5,6]])", "expect": "12" },
      { "call": "min_path_sum([[5]])", "expect": "5" },
      { "call": "min_path_sum([[1,2],[1,1]])", "expect": "3" }
    ],
    "casesHidden": 2
  }
}
