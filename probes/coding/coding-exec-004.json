{
  "id": "coding-exec-004",
  "generation": "gen-2026-09-08b",
  "subIntent": "quick",
  "prompt": "Write a Python function find_kth_largest(nums: list[int], k: int) -> int that returns the kth largest element in an unsorted list. 1-indexed: k=1 means the largest element. Do NOT use built-in sorting directly. Must handle duplicates correctly.",
  "language": "python",
  "entry": "find_kth_largest",
  "harness": {
    "setup": "from solution import find_kth_largest",
    "cases": [
      { "call": "find_kth_largest([3,2,1,5,6,4], 2)", "expect": "5" },
      { "call": "find_kth_largest([3,2,3,1,2,4,5,5,6], 4)", "expect": "4" },
      { "call": "find_kth_largest([1], 1)", "expect": "1" },
      { "call": "find_kth_largest([-1, -3, -2], 2)", "expect": "-2" }
    ],
    "casesHidden": 2
  }
}
